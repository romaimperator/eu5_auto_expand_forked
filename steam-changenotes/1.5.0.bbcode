Version 1.5.0
[list]
[*]adds support for auto-expanding buildings that support multiple levels but don't produce anything (e.g. marketplaces, granaries, etc.)
[*]fixes an issue where the RGO auto-expand button tooltip would display incorrect information about the RGO's employment level
[/list]
