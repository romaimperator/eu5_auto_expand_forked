basic_dialog = {
	name = "cm_construct_queue_window"
	widgetid = cm_construct_queue_window
	layer = top
	visible = "[And(GetPlayer.Exists, GetScriptedGui('cm_should_construct').IsShown(GuiScope.SetRoot(GetPlayer.MakeScope).End))]"
	parentanchor = center
	position = { -10000 1 }
	blockoverride "header_text" {
		raw_text = "CM: Construct Queue Window"
	}
	blockoverride "content" {
		widget = {
			state = {
				name = cm_q_wait
				trigger_on_create = yes
				next = cm_q_apply
				on_finish = "[PdxGuiWidget.TriggerAnimation('cm_q_apply')]"
			}
			state = {
				name = cm_q_apply
				duration = 2
				on_finish = "[GetScriptedGui('cm_clear_should_construct').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
				next = cm_q_wait
			}
			# Metric-gated building constructs: per-location list of building types.
			widget = {
				datamodel = "[GetPlayer.MakeScope.GetList('cm_q_locations')]"
				item = {
					widget = {
						datacontext = "[Scope.GetLocation]"
						datamodel = "[Location.MakeScope.GetList('cm_q_building_types')]"
						item = {
							widget = {
								datacontext = "[Scope.GetBuildingType]"
								state = {
									name = cm_q_construct
									trigger_on_create = yes
									on_finish = "[GetScriptedGui('cm_gui_try_build').Execute(GuiScope.SetRoot(Location.MakeScope).AddScope('building_type', BuildingType.MakeScope).AddScope('country', GetPlayer.MakeScope).AddScope('cost', MakeScopeValue(GetBuildOrExpandBuildingCost(BuildingType.Self, Location.Self))).AddScope('income', MakeScopeValue(GetBuildingTypeIncomeToOwnerInLocation(BuildingType.Self, Location.Self))).AddScope('profit', MakeScopeValue(GetBuildingTypeProfitInLocation(BuildingType.Self, Location.Self))).End)]"
								}
							}
						}
					}
				}
			}
			# Obsolete-building upgrades: per-location list of obsolete types, replaced with the engine's next tier.
			widget = {
				datamodel = "[GetPlayer.MakeScope.GetList('cm_gui_upgrade_locations')]"
				item = {
					widget = {
						datacontext = "[Scope.GetLocation]"
						datamodel = "[Location.MakeScope.GetList('cm_gui_upgrade_building_types')]"
						item = {
							widget = {
								datacontext = "[Scope.GetBuildingType]"
								state = {
									name = cm_q_construct_upgrade
									trigger_on_create = yes
									on_finish = "[GetScriptedGui('cm_gui_construct_upgrade').Execute(GuiScope.SetRoot(Location.MakeScope).AddScope('old_building_type', BuildingType.MakeScope).AddScope('new_building_type', BuildingType.GetNextReplacementBuilding(Player.Self).MakeScope).AddScope('building_type', BuildingType.GetNextReplacementBuilding(Player.Self).MakeScope).AddScope('country', GetPlayer.MakeScope).AddScope('cost', MakeScopeValue(GetBuildOrExpandBuildingCost(BuildingType.GetNextReplacementBuilding(Player.Self), Location.Self))).AddScope('income', MakeScopeValue(GetBuildingTypeIncomeToOwnerInLocation(BuildingType.GetNextReplacementBuilding(Player.Self), Location.Self))).AddScope('profit', MakeScopeValue(GetBuildingTypeProfitInLocation(BuildingType.GetNextReplacementBuilding(Player.Self), Location.Self))).End)]"
								}
							}
						}
					}
				}
			}
			# Ungated building constructs (auto-build, capital): per-location list of building types.
			widget = {
				datamodel = "[GetPlayer.MakeScope.GetList('cm_q_ungated_locations')]"
				item = {
					widget = {
						datacontext = "[Scope.GetLocation]"
						datamodel = "[Location.MakeScope.GetList('cm_q_ungated_building_types')]"
						item = {
							widget = {
								datacontext = "[Scope.GetBuildingType]"
								state = {
									name = cm_q_construct_ungated
									trigger_on_create = yes
									on_finish = "[GetScriptedGui('cm_gui_try_ungated').Execute(GuiScope.SetRoot(Location.MakeScope).AddScope('building_type', BuildingType.MakeScope).AddScope('country', GetPlayer.MakeScope).AddScope('cost', MakeScopeValue(GetBuildOrExpandBuildingCost(BuildingType.Self, Location.Self))).End)]"
								}
							}
						}
					}
				}
			}
			# Urbanize rank upgrades: per-location list of upgrade building types.
			widget = {
				datamodel = "[GetPlayer.MakeScope.GetList('cm_q_urbanize_locations')]"
				item = {
					widget = {
						datacontext = "[Scope.GetLocation]"
						datamodel = "[Location.MakeScope.GetList('cm_q_urbanize_building_types')]"
						item = {
							widget = {
								datacontext = "[Scope.GetBuildingType]"
								state = {
									name = cm_q_construct_urbanize
									trigger_on_create = yes
									on_finish = "[GetScriptedGui('cm_gui_try_urbanize').Execute(GuiScope.SetRoot(Location.MakeScope).AddScope('building_type', BuildingType.MakeScope).AddScope('country', GetPlayer.MakeScope).AddScope('cost', MakeScopeValue(GetBuildOrExpandBuildingCost(BuildingType.Self, Location.Self))).End)]"
								}
							}
						}
					}
				}
			}
			# Food buildings: per-location list of the chosen food building type.
			widget = {
				datamodel = "[GetPlayer.MakeScope.GetList('cm_q_food_locations')]"
				item = {
					widget = {
						datacontext = "[Scope.GetLocation]"
						datamodel = "[Location.MakeScope.GetList('cm_q_food_building_types')]"
						item = {
							widget = {
								datacontext = "[Scope.GetBuildingType]"
								state = {
									name = cm_q_construct_food
									trigger_on_create = yes
									on_finish = "[GetScriptedGui('cm_gui_try_food_building').Execute(GuiScope.SetRoot(Location.MakeScope).AddScope('building_type', BuildingType.MakeScope).AddScope('country', GetPlayer.MakeScope).End)]"
								}
							}
						}
					}
				}
			}
			# Food RGO upgrades: per-location; the executor re-checks slots and the food gold reserve.
			widget = {
				datamodel = "[GetPlayer.MakeScope.GetList('cm_q_food_rgo_locations')]"
				item = {
					widget = {
						datacontext = "[Scope.GetLocation]"
						state = {
							name = cm_q_construct_food_rgo
							trigger_on_create = yes
							on_finish = "[GetScriptedGui('cm_gui_try_food_rgo').Execute(GuiScope.SetRoot(Location.MakeScope).AddScope('country', GetPlayer.MakeScope).End)]"
						}
					}
				}
			}
			# RGO upgrades: per-location; the executor re-checks eligibility and gold.
			widget = {
				datamodel = "[GetPlayer.MakeScope.GetList('cm_q_rgo_locations')]"
				item = {
					widget = {
						datacontext = "[Scope.GetLocation]"
						state = {
							name = cm_q_construct_rgo
							trigger_on_create = yes
							on_finish = "[GetScriptedGui('cm_gui_try_rgo').Execute(GuiScope.SetRoot(Location.MakeScope).AddScope('location', Location.MakeScope).AddScope('country', GetPlayer.MakeScope).AddScope('profit', MakeScopeValue(Location.GetRGOProfitPerLevel)).AddScope('income', MakeScopeValue(Multiply_CFixedPoint(Location.GetRGOProfitPerLevel, Divide_CFixedPoint(Location.GetTotalIncome, Location.GetTotalPossibleTaxBase)))).End)]"
						}
					}
				}
			}
		}
	}
}
