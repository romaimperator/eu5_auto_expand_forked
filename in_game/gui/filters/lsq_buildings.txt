building_only_auto_expand = {
	scope = building
	tag = building
	group = 9
	exclusive_group = yes
	trigger = {
    location = { is_target_in_variable_list = { name = lsq_auto_expand_registered_building_types target = prev.building_type } }
	}
}

building_only_not_auto_expand = {
	scope = building
	tag = building
	group = 9
	exclusive_group = yes
	trigger = {
		location = { NOT = { is_target_in_variable_list = { name = lsq_auto_expand_registered_building_types target = prev.building_type } } }
	}
}
