lsq_max_guess_at_number_of_locations = 1000000

# Root is country. Expects scope building_type. Returns required RGO/lumber levels before auto-building this improvement (sawmill/windmill/local_smelters).
lsq_get_required_rgo_levels_before_improvement_building = {
	if = { limit = { has_variable_map = lsq_min_rgos_by_building_type is_key_in_variable_map = { name = lsq_min_rgos_by_building_type target = scope:building_type } }
		value = "variable_map(lsq_min_rgos_by_building_type|scope:building_type)"
	} else = { value = 0 }
}

# Root is the country
lsq_get_allowed_building_overbuild_levels = {
	if = { limit = { exists = var:lsq_allowed_building_overbuild_levels }
		value = var:lsq_allowed_building_overbuild_levels
	} else = { value = 0 }
}

# Root is the location.
lsq_get_remaining_free_building_levels_allowed = {
	value = modifier:free_building_levels
  owner = { add = lsq_get_allowed_building_overbuild_levels }
	subtract = total_building_levels
}

# Root is the location
lsq_get_available_building_levels = {
  value = modifier:free_building_levels
  subtract = total_building_levels
}

lsq_local_pop_promotion_speed = {
  value = modifier:local_pop_promotion_speed
}

lsq_local_pop_promotion_speed_modifier = {
  value = modifier:local_pop_promotion_speed_modifier
}

lsq_local_pop_promotion_speed_scaled = {
  value = modifier:local_pop_promotion_speed_scaled
}

lsq_global_pop_promotion_speed = {
  value = modifier:global_pop_promotion_speed
}

lsq_global_pop_promotion_speed_modifier = {
  value = modifier:global_pop_promotion_speed_modifier
}

# Root is the location
lsq_combined_pop_promotion_speed = {
  value = modifier:local_pop_promotion_speed
  owner = { add = modifier:global_pop_promotion_speed }
  add = {
    value = modifier:local_pop_promotion_speed_scaled
    multiply = population
  }
  multiply = {
    value = 1
    owner = { add = modifier:global_pop_promotion_speed_modifier }
    add = modifier:local_pop_promotion_speed_modifier
  }
}

# Root is the location
lsq_combined_pop_promotion_speed_for_nobles = {
  value = lsq_combined_pop_promotion_speed
  multiply = 0.1
}

# Root is the location
lsq_combined_pop_promotion_speed_for_clergy = {
  value = lsq_combined_pop_promotion_speed
  multiply = 0.1
}

# Root is the location
lsq_combined_pop_promotion_speed_for_burghers = {
  value = lsq_combined_pop_promotion_speed
  multiply = 0.5
}

# Root is the location
lsq_combined_pop_promotion_speed_for_laborers = {
  value = lsq_combined_pop_promotion_speed
  multiply = 1.5
}

# Root is the location
lsq_combined_pop_promotion_speed_for_soldiers = {
  value = lsq_combined_pop_promotion_speed
  multiply = 1.0
}

# Root is the location
lsq_combined_pop_promotion_speed_for_peasants = {
  value = lsq_combined_pop_promotion_speed
  multiply = 0.5
}

# Population growth modifier wrappers. Pop growth rate percentages are yearly.
# Root is the location
lsq_local_population_growth = { value = modifier:local_population_growth }
lsq_local_nobles_pop_growth = { value = modifier:local_nobles_pop_growth }
lsq_local_clergy_pop_growth = { value = modifier:local_clergy_pop_growth }
lsq_local_burghers_pop_growth = { value = modifier:local_burghers_pop_growth }
lsq_local_laborers_pop_growth = { value = modifier:local_laborers_pop_growth }
lsq_local_soldiers_pop_growth = { value = modifier:local_soldiers_pop_growth }
lsq_local_peasants_pop_growth = { value = modifier:local_peasants_pop_growth }
lsq_local_slaves_pop_growth = { value = modifier:local_slaves_pop_growth }
lsq_local_tribesmen_pop_growth = { value = modifier:local_tribesmen_pop_growth }

# Root is country
lsq_global_population_growth = { value = modifier:global_population_growth }
lsq_global_nobles_pop_growth = { value = modifier:global_nobles_pop_growth }
lsq_global_clergy_pop_growth = { value = modifier:global_clergy_pop_growth }
lsq_global_burghers_pop_growth = { value = modifier:global_burghers_pop_growth }
lsq_global_laborers_pop_growth = { value = modifier:global_laborers_pop_growth }
lsq_global_soldiers_pop_growth = { value = modifier:global_soldiers_pop_growth }
lsq_global_peasants_pop_growth = { value = modifier:global_peasants_pop_growth }
lsq_global_slaves_pop_growth = { value = modifier:global_slaves_pop_growth }
lsq_global_tribesmen_pop_growth = { value = modifier:global_tribesmen_pop_growth }

# Root is the location
lsq_base_location_pop_growth_rate = {
  value = modifier:local_population_growth
  owner = { add = modifier:global_population_growth }
}

# Per-pop-type yearly growth rate. Root is the location.
lsq_location_nobles_pop_growth_rate = {
  value = lsq_base_location_pop_growth_rate
  multiply = {
    value = 1
    add = modifier:local_nobles_pop_growth
    owner = { add = modifier:global_nobles_pop_growth }
  }
}
lsq_location_clergy_pop_growth_rate = {
  value = lsq_base_location_pop_growth_rate
  multiply = {
    value = 1
    add = modifier:local_clergy_pop_growth
    owner = { add = modifier:global_clergy_pop_growth }
  }
}
lsq_location_burghers_pop_growth_rate = {
  value = lsq_base_location_pop_growth_rate
  multiply = {
    value = 1
    add = modifier:local_burghers_pop_growth
    owner = { add = modifier:global_burghers_pop_growth }
  }
}
lsq_location_laborers_pop_growth_rate = {
  value = lsq_base_location_pop_growth_rate
  multiply = {
    value = 1
    add = modifier:local_laborers_pop_growth
    owner = { add = modifier:global_laborers_pop_growth }
  }
}
lsq_location_soldiers_pop_growth_rate = {
  value = lsq_base_location_pop_growth_rate
  multiply = {
    value = 1
    add = modifier:local_soldiers_pop_growth
    owner = { add = modifier:global_soldiers_pop_growth }
  }
}
lsq_location_peasants_pop_growth_rate = {
  value = lsq_base_location_pop_growth_rate
  multiply = {
    value = 1
    add = modifier:local_peasants_pop_growth
    owner = { add = modifier:global_peasants_pop_growth }
  }
}
lsq_location_slaves_pop_growth_rate = {
  value = lsq_base_location_pop_growth_rate
  multiply = {
    value = 1
    add = modifier:local_slaves_pop_growth
    owner = { add = modifier:global_slaves_pop_growth }
  }
}
lsq_location_tribesmen_pop_growth_rate = {
  value = lsq_base_location_pop_growth_rate
  multiply = {
    value = 1
    add = modifier:local_tribesmen_pop_growth
    owner = { add = modifier:global_tribesmen_pop_growth }
  }
}

# Per-pop-type expected new pops next month. Yearly growth rate divided by 12.
# Root is the location.
lsq_location_expected_new_nobles = {
  value = num_pop_type:nobles
  multiply = lsq_location_nobles_pop_growth_rate
  divide = 12
}
lsq_location_expected_new_clergy = {
  value = num_pop_type:clergy
  multiply = lsq_location_clergy_pop_growth_rate
  divide = 12
}
lsq_location_expected_new_burghers = {
  value = num_pop_type:burghers
  multiply = lsq_location_burghers_pop_growth_rate
  divide = 12
}
lsq_location_expected_new_laborers = {
  value = num_pop_type:laborers
  multiply = lsq_location_laborers_pop_growth_rate
  divide = 12
}
lsq_location_expected_new_soldiers = {
  value = num_pop_type:soldiers
  multiply = lsq_location_soldiers_pop_growth_rate
  divide = 12
}
lsq_location_expected_new_peasants = {
  value = num_pop_type:peasants
  multiply = lsq_location_peasants_pop_growth_rate
  divide = 12
}
lsq_location_expected_new_slaves = {
  value = num_pop_type:slaves
  multiply = lsq_location_slaves_pop_growth_rate
  divide = 12
}
lsq_location_expected_new_tribesmen = {
  value = num_pop_type:tribesmen
  multiply = lsq_location_tribesmen_pop_growth_rate
  divide = 12
}

# Combined monthly pop additions: promotion speed plus new pop growth.
# Slaves and tribesmen aren't promoted, so only growth contributes.
# Root is the location.
lsq_combined_monthly_new_pops_of_type_nobles = {
  value = lsq_combined_pop_promotion_speed_for_nobles
  add = lsq_location_expected_new_nobles
}
lsq_combined_monthly_new_pops_of_type_clergy = {
  value = lsq_combined_pop_promotion_speed_for_clergy
  add = lsq_location_expected_new_clergy
}
lsq_combined_monthly_new_pops_of_type_burghers = {
  value = lsq_combined_pop_promotion_speed_for_burghers
  add = lsq_location_expected_new_burghers
}
lsq_combined_monthly_new_pops_of_type_laborers = {
  value = lsq_combined_pop_promotion_speed_for_laborers
  add = lsq_location_expected_new_laborers
}
lsq_combined_monthly_new_pops_of_type_soldiers = {
  value = lsq_combined_pop_promotion_speed_for_soldiers
  add = lsq_location_expected_new_soldiers
}
lsq_combined_monthly_new_pops_of_type_peasants = {
  value = lsq_combined_pop_promotion_speed_for_peasants
  add = lsq_location_expected_new_peasants
}
lsq_combined_monthly_new_pops_of_type_slaves = {
  value = lsq_location_expected_new_slaves
}
lsq_combined_monthly_new_pops_of_type_tribesmen = {
  value = lsq_location_expected_new_tribesmen
}
lsq_combined_monthly_new_pops_of_type_laborers_or_slaves = {
  value = lsq_combined_monthly_new_pops_of_type_laborers
  add = lsq_combined_monthly_new_pops_of_type_slaves
}

lsq_local_laborers_desired_pop = {
  value = modifier:local_laborers_desired_pop
}
lsq_local_laborers_desired_pop_scaled = {
  value = modifier:local_laborers_desired_pop_scaled
}
lsq_global_laborers_desired_pop = {
  value = modifier:global_laborers_desired_pop
}
lsq_global_laborers_desired_pop_scaled = {
  value = modifier:global_laborers_desired_pop_scaled
}
lsq_global_laborers_rural_desired_pop = {
  value = modifier:global_laborers_rural_desired_pop
}
lsq_global_laborers_rural_desired_pop_scaled = {
  value = modifier:global_laborers_rural_desired_pop_scaled
}
lsq_global_laborers_city_desired_pop = {
  value = modifier:global_laborers_city_desired_pop
}
lsq_global_laborers_city_desired_pop_scaled = {
  value = modifier:global_laborers_city_desired_pop_scaled
}

# Root is the location
lsq_location_nobles_needed_for_buildings = {
  value = 0
  every_buildings_in_location = {
    limit = { building_pop_type = nobles }
    save_temporary_scope_as = building
    add = lsq_building_employment_required
  }
}

# Root is the location
lsq_location_nobles_available = {
  value = num_pop_type:nobles
  subtract = lsq_location_nobles_needed_for_buildings
}

# Root is the location
lsq_location_clergy_needed_for_buildings = {
  value = 0
  every_buildings_in_location = {
    limit = { building_pop_type = clergy }
    save_temporary_scope_as = building
    add = lsq_building_employment_required
  }
}

# Root is the location
lsq_location_clergy_available = {
  value = num_pop_type:clergy
  subtract = lsq_location_clergy_needed_for_buildings
}

# Root is the location
lsq_location_burghers_needed_for_buildings = {
  value = 0
  every_buildings_in_location = {
    limit = { building_pop_type = burghers }
    save_temporary_scope_as = building
    add = lsq_building_employment_required
  }
}

# Root is the location
lsq_location_burghers_available = {
  value = num_pop_type:burghers
  subtract = lsq_location_burghers_needed_for_buildings
}

# Root is the location
lsq_location_laborers_needed_for_buildings = {
  value = 0
  every_buildings_in_location = {
    limit = { building_pop_type = laborers }
    save_temporary_scope_as = building
    add = lsq_building_employment_required
  }
}

lsq_location_nobles_pop_count = { value = num_pop_type:nobles }
lsq_location_clergy_pop_count = { value = num_pop_type:clergy }
lsq_location_burghers_pop_count = { value = num_pop_type:burghers }
lsq_location_laborers_pop_count = { value = num_pop_type:laborers }
lsq_location_soldiers_pop_count = { value = num_pop_type:soldiers }
lsq_location_peasants_pop_count = { value = num_pop_type:peasants }
lsq_location_slaves_pop_count = { value = num_pop_type:slaves }
lsq_location_tribesmen_pop_count = { value = num_pop_type:tribesmen }

# Root is the location
lsq_location_laborers_available = {
  value = num_pop_type:laborers
  subtract = lsq_location_laborers_needed_for_buildings
  subtract = {
    value = lsq_modifier_rgo_level
    add = lsq_get_rgos_under_construction
    subtract = num_pop_type:slaves
  }
}

# Root is the location
lsq_location_soldiers_needed_for_buildings = {
  value = 0
  every_buildings_in_location = {
    limit = { building_pop_type = soldiers }
    save_temporary_scope_as = building
    add = lsq_building_employment_required
  }
}

# Root is the location
lsq_location_soldiers_available = {
  value = num_pop_type:soldiers
  subtract = lsq_location_soldiers_needed_for_buildings
}

# Root is the location
lsq_location_peasants_needed_for_buildings = {
  value = 0
  every_buildings_in_location = {
    limit = { building_pop_type = peasants }
    save_temporary_scope_as = building
    add = lsq_building_employment_required
  }
}

# Root is the location
lsq_location_peasants_available = {
  value = num_pop_type:peasants
  subtract = lsq_location_peasants_needed_for_buildings
}

# Root is the location
lsq_location_slaves_needed_for_buildings = {
  value = 0
  every_buildings_in_location = {
    limit = { building_pop_type = slaves }
    save_temporary_scope_as = building
    add = lsq_building_employment_required
  }
}

# Root is the location
lsq_location_slaves_available = {
  value = num_pop_type:slaves
  subtract = lsq_location_slaves_needed_for_buildings
}

# Root is the location. Combined laborer+slave pool minus the jobs either pop type can fill.
lsq_location_laborers_or_slaves_available = {
  value = num_pop_type:laborers
  add = num_pop_type:slaves
  subtract = lsq_location_laborers_needed_for_buildings
  subtract = lsq_location_slaves_needed_for_buildings
  subtract = lsq_modifier_rgo_level
  subtract = lsq_get_rgos_under_construction
}

# Root is the location
lsq_location_tribesmen_needed_for_buildings = {
  value = 0
  every_buildings_in_location = {
    limit = { building_pop_type = tribesmen }
    save_temporary_scope_as = building
    add = lsq_building_employment_required
  }
}

# Root is the location
lsq_location_tribesmen_available = {
  value = num_pop_type:tribesmen
  subtract = lsq_location_tribesmen_needed_for_buildings
}

# Includes potential employment from levels under construction and from buildings that are subsidized but not fully employed
# Expects scope: building
lsq_building_employment_required = {
  value = 0
  scope:building = {
    if = { limit = { is_opened = yes }
      if = { limit = { AND = { building_type = { lsq_is_building_type_production_building = yes } is_subsidized = yes } }
        add = {
          value = building_level
          multiply = building_type.employment_size
        }
      } else = {
        add = building_employment_size_amount
      }
      add = {
        value = building_levels_under_construction
        multiply = building_type.employment_size
      }
    }
  }
}

lsq_location_lumber_mill_level = {
  if = { limit = { has_building = building_type:lumber_mill }
    "building(building_type:lumber_mill|owner)" = { value = building_level }
  } else = {
    value = -1
  }
}
