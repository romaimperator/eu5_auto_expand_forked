# AUTO-GENERATED FILE. DO NOT EDIT BY HAND.
# Generated by tools/compile_templates.rb
# Sources:
# - <%= game_root.join("in_game/common/building_types/*.txt") %>
# - <%= game_root.join("in_game/common/goods_demand/building_construction_costs.txt") %>
# - <%= game_root.join("in_game/common/goods_demand/special_construction_demands.txt") %>

# Expects scopes: building_type
lsq_get_non_standard_building_type_construction_demand_total = {
  value = 0
<% construction_goods.each do |good| -%>
  if = { limit = { AND = { has_global_variable_map = lsq_non_standard_building_type_construction_demand_<%= good %> is_key_in_global_variable_map = { name = lsq_non_standard_building_type_construction_demand_<%= good %> target = scope:building_type } } }
    add = "global_variable_map(lsq_non_standard_building_type_construction_demand_<%= good %>|scope:building_type)"
  }
<% end -%>
}

# Expects scopes: building_type
lsq_get_non_standard_building_type_construction_demand_total_count = {
  value = 0
<% construction_goods.each do |good| -%>
  if = { limit = { AND = { has_global_variable_map = lsq_non_standard_building_type_construction_demand_<%= good %> is_key_in_global_variable_map = { name = lsq_non_standard_building_type_construction_demand_<%= good %> target = scope:building_type } } } add = 1 }
<% end -%>
}
