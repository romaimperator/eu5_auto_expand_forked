#######################
# Directly controlled #
#######################

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_allowed_to_build = {
  value = 0
  if = { limit = { scope:building_type = building_type:temple }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_temple = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:library }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_library = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_settlement_building = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_bridge_infrastructure = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_pound_lock_canal_infrastructure = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:university }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_university = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:hospital }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_hospital = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_minting_office = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_counting_house = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:wharf }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_wharf = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:market_village }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_market_village = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_sawmill = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:windmill }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_windmill = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_local_smelters = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_protected_harbor = yes }
      add = 1
    }
  }
}

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_will_auto_build = {
  value = 0
  if = { limit = { scope:building_type = building_type:temple }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_temple = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:library }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_library = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_settlement_building = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_bridge_infrastructure = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_pound_lock_canal_infrastructure = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:university }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_university = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:hospital }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_hospital = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_minting_office = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_counting_house = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:wharf }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_wharf = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:market_village }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_market_village = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_sawmill = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:windmill }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_windmill = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_local_smelters = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_protected_harbor = yes }
      add = 1
    }
  }
}

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_will_auto_build_cached = {
  if = { limit = { scope:building_type = building_type:temple }
    value = var:lsq_all_controlled_locations_size_will_auto_build_temple
  } else_if = { limit = { scope:building_type = building_type:library }
    value = var:lsq_all_controlled_locations_size_will_auto_build_library
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    value = var:lsq_all_controlled_locations_size_will_auto_build_settlement_building
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    value = var:lsq_all_controlled_locations_size_will_auto_build_bridge_infrastructure
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    value = var:lsq_all_controlled_locations_size_will_auto_build_pound_lock_canal_infrastructure
  } else_if = { limit = { scope:building_type = building_type:university }
    value = var:lsq_all_controlled_locations_size_will_auto_build_university
  } else_if = { limit = { scope:building_type = building_type:hospital }
    value = var:lsq_all_controlled_locations_size_will_auto_build_hospital
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    value = var:lsq_all_controlled_locations_size_will_auto_build_minting_office
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    value = var:lsq_all_controlled_locations_size_will_auto_build_counting_house
  } else_if = { limit = { scope:building_type = building_type:wharf }
    value = var:lsq_all_controlled_locations_size_will_auto_build_wharf
  } else_if = { limit = { scope:building_type = building_type:market_village }
    value = var:lsq_all_controlled_locations_size_will_auto_build_market_village
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    value = var:lsq_all_controlled_locations_size_will_auto_build_sawmill
  } else_if = { limit = { scope:building_type = building_type:windmill }
    value = var:lsq_all_controlled_locations_size_will_auto_build_windmill
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    value = var:lsq_all_controlled_locations_size_will_auto_build_local_smelters
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    value = var:lsq_all_controlled_locations_size_will_auto_build_protected_harbor
  }
}


#####################
# Includes subjects #
#####################

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_including_subjects_allowed_to_build = {
  value = lsq_auto_build_number_of_locations_allowed_to_build
  if = { limit = { scope:building_type = building_type:temple }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_temple = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:library }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_library = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_settlement_building = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_bridge_infrastructure = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_pound_lock_canal_infrastructure = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:university }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_university = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:hospital }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_hospital = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_minting_office = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_counting_house = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:wharf }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_wharf = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:market_village }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_market_village = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_sawmill = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:windmill }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_windmill = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_local_smelters = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_protected_harbor = yes }
        add = 1
      }
    }
  }
}

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_including_subjects_will_auto_build = {
  value = lsq_auto_build_number_of_locations_will_auto_build
  if = { limit = { scope:building_type = building_type:temple }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_temple = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:library }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_library = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_settlement_building = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_bridge_infrastructure = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_pound_lock_canal_infrastructure = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:university }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_university = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:hospital }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_hospital = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_minting_office = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_counting_house = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:wharf }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_wharf = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:market_village }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_market_village = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_sawmill = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:windmill }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_windmill = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_local_smelters = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_protected_harbor = yes }
        add = 1
      }
    }
  }
}

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_including_subjects_will_auto_build_cached = {
  if = { limit = { scope:building_type = building_type:temple }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_temple
  } else_if = { limit = { scope:building_type = building_type:library }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_library
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_settlement_building
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_bridge_infrastructure
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_pound_lock_canal_infrastructure
  } else_if = { limit = { scope:building_type = building_type:university }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_university
  } else_if = { limit = { scope:building_type = building_type:hospital }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_hospital
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_minting_office
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_counting_house
  } else_if = { limit = { scope:building_type = building_type:wharf }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_wharf
  } else_if = { limit = { scope:building_type = building_type:market_village }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_market_village
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_sawmill
  } else_if = { limit = { scope:building_type = building_type:windmill }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_windmill
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_local_smelters
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    value = var:lsq_all_locations_including_subjects_size_will_auto_build_protected_harbor
  }
}

# Root is the building type
# Expects scopes: country
lsq_get_minimum_control_for_auto_build = {
  scope:country = {
    if = { limit = { AND = { has_variable_map = lsq_min_control_by_building_type is_key_in_variable_map = { name = lsq_min_control_by_building_type target = prev } } }
      value = "variable_map(lsq_min_control_by_building_type|prev)"
    } else = { value = -1 }
  }
}

# Root is the building type
# Expects scopes: country
lsq_get_minimum_market_price_impact_for_auto_build = {
  scope:country = {
    if = { limit = { AND = { has_variable_map = lsq_min_discount_by_building_type is_key_in_variable_map = { name = lsq_min_discount_by_building_type target = prev } } }
      value = "variable_map(lsq_min_discount_by_building_type|prev)"
    } else = { value = -1 }
  }
}
