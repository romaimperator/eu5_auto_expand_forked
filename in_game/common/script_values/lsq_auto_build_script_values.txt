#######################
# Directly controlled #
#######################

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_allowed_to_build = {
  value = 0
  if = { limit = { scope:building_type = building_type:temple }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_temple = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:library }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_library = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_settlement_building = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_bridge_infrastructure = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_pound_lock_canal_infrastructure = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:university }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_university = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:hospital }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_hospital = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_minting_office = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_counting_house = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:wharf }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_wharf = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:market_village }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_market_village = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_sawmill = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:windmill }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_windmill = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_local_smelters = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    every_owned_location = {
      limit = { lsq_location_allowed_to_build_protected_harbor = yes }
      add = 1
    }
  }
}

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_will_auto_build = {
  value = 0
  if = { limit = { scope:building_type = building_type:temple }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_temple = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:library }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_library = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_settlement_building = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_bridge_infrastructure = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_pound_lock_canal_infrastructure = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:university }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_university = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:hospital }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_hospital = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_minting_office = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_counting_house = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:wharf }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_wharf = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:market_village }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_market_village = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_sawmill = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:windmill }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_windmill = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_local_smelters = yes }
      add = 1
    }
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    every_owned_location = {
      limit = { lsq_location_will_auto_build_protected_harbor = yes }
      add = 1
    }
  }
}


#####################
# Includes subjects #
#####################

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_including_subjects_allowed_to_build = {
  value = lsq_auto_build_number_of_locations_allowed_to_build
  if = { limit = { scope:building_type = building_type:temple }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_temple = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:library }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_library = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_settlement_building = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_bridge_infrastructure = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_pound_lock_canal_infrastructure = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:university }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_university = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:hospital }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_hospital = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_minting_office = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_counting_house = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:wharf }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_wharf = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:market_village }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_market_village = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_sawmill = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:windmill }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_windmill = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_local_smelters = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_allowed_to_build_protected_harbor = yes }
        add = 1
      }
    }
  }
}

# Root is the country. Also expects the scope building_type and country.
lsq_auto_build_number_of_locations_including_subjects_will_auto_build = {
  value = lsq_auto_build_number_of_locations_will_auto_build
  if = { limit = { scope:building_type = building_type:temple }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_temple = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:library }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_library = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:settlement_building }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_settlement_building = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:bridge_infrastructure }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_bridge_infrastructure = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:pound_lock_canal_infrastructure }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_pound_lock_canal_infrastructure = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:university }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_university = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:hospital }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_hospital = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:minting_office }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_minting_office = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:counting_house }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_counting_house = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:wharf }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_wharf = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:market_village }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_market_village = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:sawmill }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_sawmill = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:windmill }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_windmill = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:local_smelters }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_local_smelters = yes }
        add = 1
      }
    }
  } else_if = { limit = { scope:building_type = building_type:protected_harbor }
    every_subject_or_below = {
      every_owned_location = {
        limit = { lsq_location_will_auto_build_protected_harbor = yes }
        add = 1
      }
    }
  }
}

lsq_get_minimum_control_for_auto_build = {
  scope:country = {
    if = { limit = { AND = { has_variable_map = lsq_minimum_control_for_auto_build is_key_in_variable_map = { name = lsq_minimum_control_for_auto_build target = scope:building_type } } }
      value = "variable_map(lsq_minimum_control_for_auto_build|scope:building_type)"
    } else = { value = -1 }
  }
}
