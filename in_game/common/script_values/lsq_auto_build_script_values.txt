# Root is the country. Also expects the scope building_type.
lsq_auto_build_number_of_locations_allowed_to_build = {
  value = 0
  every_controlled_location = {
    limit = { lsq_location_allowed_to_build = { target = scope:building_type } }
    add = 1
  }
}

# Root is the country. Also expects the scope building_type.
lsq_auto_build_number_of_locations_will_auto_build = {
  value = 0
  every_controlled_location = {
    limit = { lsq_auto_build_will_auto_build = { target = scope:building_type } }
    add = 1
  }
}
