# AUTO-GENERATED FILE. DO NOT EDIT BY HAND.
# Generated by tools/compile_templates.rb
# Sources:
# - <%= game_root.join("in_game/common/building_types/*.txt") %>
# - <%= game_root.join("in_game/common/goods_demand/building_construction_costs.txt") %>
# - <%= game_root.join("in_game/common/goods_demand/special_construction_demands.txt") %>

# Root is the building_type
# Expects scopes: location
lsq_get_increase_per_level_cost_for_building_type = {
<% if building_ids.empty? -%>
	value = 0
<% else -%>
<% building_ids.select { |building_type| get_increase_per_level_cost(building_type) != "0" }.each_with_index do |building_type, index| -%>
	<%= index.zero? ? "if" : "else_if" %> = { limit = { this = building_type:<%= building_type %> } value = <%= get_increase_per_level_cost(building_type) %> }
<% end -%>
	else = { value = 0 }
<% end -%>
  scope:location = { multiply = { value = 1 add = "location_building_level(prev)" } }
}
