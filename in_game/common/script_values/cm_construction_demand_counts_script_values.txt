# Construction demand totals using per-good global variable maps.
# These maps are populated by the hidden window GUI for all building types.
# The 28 construction goods below are a stable game constant.
# Expects scopes: building_type
cm_get_construction_demand_total = {
  value = 0
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_books is_key_in_global_variable_map = { name = cm_construction_demand_books target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_books|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_clay is_key_in_global_variable_map = { name = cm_construction_demand_clay target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_clay|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_cloth is_key_in_global_variable_map = { name = cm_construction_demand_cloth target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_cloth|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_copper is_key_in_global_variable_map = { name = cm_construction_demand_copper target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_copper|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_fine_cloth is_key_in_global_variable_map = { name = cm_construction_demand_fine_cloth target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_fine_cloth|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_firearms is_key_in_global_variable_map = { name = cm_construction_demand_firearms target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_firearms|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_glass is_key_in_global_variable_map = { name = cm_construction_demand_glass target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_glass|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_goods_gold is_key_in_global_variable_map = { name = cm_construction_demand_goods_gold target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_goods_gold|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_horses is_key_in_global_variable_map = { name = cm_construction_demand_horses target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_horses|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_jewelry is_key_in_global_variable_map = { name = cm_construction_demand_jewelry target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_jewelry|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_leather is_key_in_global_variable_map = { name = cm_construction_demand_leather target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_leather|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_liquor is_key_in_global_variable_map = { name = cm_construction_demand_liquor target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_liquor|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_livestock is_key_in_global_variable_map = { name = cm_construction_demand_livestock target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_livestock|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_lumber is_key_in_global_variable_map = { name = cm_construction_demand_lumber target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_lumber|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_marble is_key_in_global_variable_map = { name = cm_construction_demand_marble target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_marble|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_masonry is_key_in_global_variable_map = { name = cm_construction_demand_masonry target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_masonry|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_naval_supplies is_key_in_global_variable_map = { name = cm_construction_demand_naval_supplies target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_naval_supplies|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_paper is_key_in_global_variable_map = { name = cm_construction_demand_paper target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_paper|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_salt is_key_in_global_variable_map = { name = cm_construction_demand_salt target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_salt|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_sand is_key_in_global_variable_map = { name = cm_construction_demand_sand target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_sand|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_silver is_key_in_global_variable_map = { name = cm_construction_demand_silver target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_silver|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_steel is_key_in_global_variable_map = { name = cm_construction_demand_steel target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_steel|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_stone is_key_in_global_variable_map = { name = cm_construction_demand_stone target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_stone|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_tin is_key_in_global_variable_map = { name = cm_construction_demand_tin target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_tin|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_tools is_key_in_global_variable_map = { name = cm_construction_demand_tools target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_tools|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_weaponry is_key_in_global_variable_map = { name = cm_construction_demand_weaponry target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_weaponry|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_wheat is_key_in_global_variable_map = { name = cm_construction_demand_wheat target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_wheat|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_wool is_key_in_global_variable_map = { name = cm_construction_demand_wool target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_wool|scope:cm_building_type)"
  }
}

# Standard construction demand total: lumber, masonry, glass, sand, stone.
# Non-standard goods such as gold, tools, and paper are excluded because their prices
# are not depressible by the player and should not gate build decisions.
# Expects scopes: building_type
cm_get_standard_construction_demand_total = {
  value = 0
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_glass is_key_in_global_variable_map = { name = cm_construction_demand_glass target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_glass|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_lumber is_key_in_global_variable_map = { name = cm_construction_demand_lumber target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_lumber|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_masonry is_key_in_global_variable_map = { name = cm_construction_demand_masonry target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_masonry|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_sand is_key_in_global_variable_map = { name = cm_construction_demand_sand target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_sand|scope:cm_building_type)"
  }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_stone is_key_in_global_variable_map = { name = cm_construction_demand_stone target = scope:cm_building_type } } }
    add = "global_variable_map(cm_construction_demand_stone|scope:cm_building_type)"
  }
}

# Expects scopes: building_type
cm_get_construction_demand_total_count = {
  value = 0
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_books is_key_in_global_variable_map = { name = cm_construction_demand_books target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_clay is_key_in_global_variable_map = { name = cm_construction_demand_clay target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_cloth is_key_in_global_variable_map = { name = cm_construction_demand_cloth target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_copper is_key_in_global_variable_map = { name = cm_construction_demand_copper target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_fine_cloth is_key_in_global_variable_map = { name = cm_construction_demand_fine_cloth target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_firearms is_key_in_global_variable_map = { name = cm_construction_demand_firearms target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_glass is_key_in_global_variable_map = { name = cm_construction_demand_glass target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_goods_gold is_key_in_global_variable_map = { name = cm_construction_demand_goods_gold target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_horses is_key_in_global_variable_map = { name = cm_construction_demand_horses target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_jewelry is_key_in_global_variable_map = { name = cm_construction_demand_jewelry target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_leather is_key_in_global_variable_map = { name = cm_construction_demand_leather target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_liquor is_key_in_global_variable_map = { name = cm_construction_demand_liquor target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_livestock is_key_in_global_variable_map = { name = cm_construction_demand_livestock target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_lumber is_key_in_global_variable_map = { name = cm_construction_demand_lumber target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_marble is_key_in_global_variable_map = { name = cm_construction_demand_marble target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_masonry is_key_in_global_variable_map = { name = cm_construction_demand_masonry target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_naval_supplies is_key_in_global_variable_map = { name = cm_construction_demand_naval_supplies target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_paper is_key_in_global_variable_map = { name = cm_construction_demand_paper target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_salt is_key_in_global_variable_map = { name = cm_construction_demand_salt target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_sand is_key_in_global_variable_map = { name = cm_construction_demand_sand target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_silver is_key_in_global_variable_map = { name = cm_construction_demand_silver target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_steel is_key_in_global_variable_map = { name = cm_construction_demand_steel target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_stone is_key_in_global_variable_map = { name = cm_construction_demand_stone target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_tin is_key_in_global_variable_map = { name = cm_construction_demand_tin target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_tools is_key_in_global_variable_map = { name = cm_construction_demand_tools target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_weaponry is_key_in_global_variable_map = { name = cm_construction_demand_weaponry target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_wheat is_key_in_global_variable_map = { name = cm_construction_demand_wheat target = scope:cm_building_type } } } add = 1 }
  if = { limit = { AND = { has_global_variable_map = cm_construction_demand_wool is_key_in_global_variable_map = { name = cm_construction_demand_wool target = scope:cm_building_type } } } add = 1 }
}
