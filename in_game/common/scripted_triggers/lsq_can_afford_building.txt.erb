# AUTO-GENERATED FILE. DO NOT EDIT BY HAND.
# Generated by tools/compile_templates.rb
# Sources:
# - <%= game_root.join("in_game/common/building_types/*.txt") %>
# - <%= game_root.join("in_game/common/goods_demand/building_construction_costs.txt") %>
# - <%= game_root.join("in_game/common/goods_demand/special_construction_demands.txt") %>

# Root is the building type, also expects scope:location
lsq_can_afford_switch_based_on_building_type = {
<% if building_ids.empty? -%>
	always = no
<% else -%>
	switch = { trigger = $target$
<% building_ids.each do |building_type| -%>
		building_type:<%= building_type %> = { lsq_can_afford_<%= building_type %> = yes }
<% end -%>
		fallback = {
			AND = {
<% construction_goods.each do |good| -%>
        trigger_if = { limit = { AND = { has_global_variable_map = lsq_non_standard_building_type_construction_demand_<%= good %> is_key_in_global_variable_map = { name = lsq_non_standard_building_type_construction_demand_<%= good %> target = scope:building_type } } }
					AND = {
						goods:<%= good %> = { save_temporary_scope_as = good }
						lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_non_standard_building_type_construction_demand_<%= good %>|scope:building_type)"
					}
				} trigger_else = { always = yes }
<% end -%>
			}
		}
	}
<% end -%>
}
<% building_ids.each do |building_type| -%>

# Root is the location
lsq_can_afford_<%= building_type %> = {
	AND = {
<% goods = resolved_building_goods.fetch(building_type, {}) -%>
<% if goods.empty? -%>
		always = yes
<% else -%>
<% sorted_goods_pairs(goods).each do |good, amount| -%>
		goods:<%= good %> = { save_temporary_scope_as = good }
		lsq_get_max_required_construction_demand_to_still_allow_building >= <%= amount %>
<% end -%>
<% end -%>
	}
}
<% end -%>
