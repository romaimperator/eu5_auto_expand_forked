# Root is the building type, also expects scope:location
# Checks if all construction goods are available in sufficient quantity.
# Construction demand amounts are populated by the hidden window GUI into per-good global variable maps.
lsq_can_afford_switch_based_on_building_type = {
	AND = {
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_books is_key_in_global_variable_map = { name = lsq_construction_demand_books target = scope:building_type } } }
			AND = {
				goods:books = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_books|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_clay is_key_in_global_variable_map = { name = lsq_construction_demand_clay target = scope:building_type } } }
			AND = {
				goods:clay = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_clay|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_cloth is_key_in_global_variable_map = { name = lsq_construction_demand_cloth target = scope:building_type } } }
			AND = {
				goods:cloth = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_cloth|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_copper is_key_in_global_variable_map = { name = lsq_construction_demand_copper target = scope:building_type } } }
			AND = {
				goods:copper = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_copper|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_fine_cloth is_key_in_global_variable_map = { name = lsq_construction_demand_fine_cloth target = scope:building_type } } }
			AND = {
				goods:fine_cloth = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_fine_cloth|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_firearms is_key_in_global_variable_map = { name = lsq_construction_demand_firearms target = scope:building_type } } }
			AND = {
				goods:firearms = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_firearms|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_glass is_key_in_global_variable_map = { name = lsq_construction_demand_glass target = scope:building_type } } }
			AND = {
				goods:glass = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_glass|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_goods_gold is_key_in_global_variable_map = { name = lsq_construction_demand_goods_gold target = scope:building_type } } }
			AND = {
				goods:goods_gold = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_goods_gold|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_horses is_key_in_global_variable_map = { name = lsq_construction_demand_horses target = scope:building_type } } }
			AND = {
				goods:horses = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_horses|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_jewelry is_key_in_global_variable_map = { name = lsq_construction_demand_jewelry target = scope:building_type } } }
			AND = {
				goods:jewelry = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_jewelry|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_leather is_key_in_global_variable_map = { name = lsq_construction_demand_leather target = scope:building_type } } }
			AND = {
				goods:leather = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_leather|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_liquor is_key_in_global_variable_map = { name = lsq_construction_demand_liquor target = scope:building_type } } }
			AND = {
				goods:liquor = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_liquor|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_livestock is_key_in_global_variable_map = { name = lsq_construction_demand_livestock target = scope:building_type } } }
			AND = {
				goods:livestock = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_livestock|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_lumber is_key_in_global_variable_map = { name = lsq_construction_demand_lumber target = scope:building_type } } }
			AND = {
				goods:lumber = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_lumber|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_marble is_key_in_global_variable_map = { name = lsq_construction_demand_marble target = scope:building_type } } }
			AND = {
				goods:marble = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_marble|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_masonry is_key_in_global_variable_map = { name = lsq_construction_demand_masonry target = scope:building_type } } }
			AND = {
				goods:masonry = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_masonry|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_naval_supplies is_key_in_global_variable_map = { name = lsq_construction_demand_naval_supplies target = scope:building_type } } }
			AND = {
				goods:naval_supplies = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_naval_supplies|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_paper is_key_in_global_variable_map = { name = lsq_construction_demand_paper target = scope:building_type } } }
			AND = {
				goods:paper = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_paper|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_salt is_key_in_global_variable_map = { name = lsq_construction_demand_salt target = scope:building_type } } }
			AND = {
				goods:salt = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_salt|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_sand is_key_in_global_variable_map = { name = lsq_construction_demand_sand target = scope:building_type } } }
			AND = {
				goods:sand = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_sand|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_silver is_key_in_global_variable_map = { name = lsq_construction_demand_silver target = scope:building_type } } }
			AND = {
				goods:silver = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_silver|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_steel is_key_in_global_variable_map = { name = lsq_construction_demand_steel target = scope:building_type } } }
			AND = {
				goods:steel = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_steel|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_stone is_key_in_global_variable_map = { name = lsq_construction_demand_stone target = scope:building_type } } }
			AND = {
				goods:stone = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_stone|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_tin is_key_in_global_variable_map = { name = lsq_construction_demand_tin target = scope:building_type } } }
			AND = {
				goods:tin = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_tin|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_tools is_key_in_global_variable_map = { name = lsq_construction_demand_tools target = scope:building_type } } }
			AND = {
				goods:tools = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_tools|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_weaponry is_key_in_global_variable_map = { name = lsq_construction_demand_weaponry target = scope:building_type } } }
			AND = {
				goods:weaponry = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_weaponry|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_wheat is_key_in_global_variable_map = { name = lsq_construction_demand_wheat target = scope:building_type } } }
			AND = {
				goods:wheat = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_wheat|scope:building_type)"
			}
		} trigger_else = { always = yes }
		trigger_if = { limit = { AND = { has_global_variable_map = lsq_construction_demand_wool is_key_in_global_variable_map = { name = lsq_construction_demand_wool target = scope:building_type } } }
			AND = {
				goods:wool = { save_temporary_scope_as = good }
				lsq_get_max_required_construction_demand_to_still_allow_building >= "global_variable_map(lsq_construction_demand_wool|scope:building_type)"
			}
		} trigger_else = { always = yes }
	}
}
