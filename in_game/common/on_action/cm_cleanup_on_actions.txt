# Monthly hook for Building Cleanup. A bare on_action key merges with the existing definition
# rather than replacing it, so this sits beside the dispatcher's leaves on the same pulse.
cmf_monthly_human_country_pulse = {
	on_actions = {
		cm_cleanup_monthly_pulse
	}
}

# Root is the country.
cm_cleanup_monthly_pulse = {
	effect = {
		cm_cu_monthly_pass = yes
	}
}
