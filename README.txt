DOCUMENTATION:
mod v1.4

all changes to vanilla files have been commentated with the cm prefix
all files with the cm_ prefix are new and do not replace any vanilla files/features, they should therefore not cause any compatibility issues

for UI-mod integration:
use cm_auto_expand_widget = {} in production_lateralview.gui
use cm_auto_expand_widget_global_construction = {}  in build_location_lateralview.gui